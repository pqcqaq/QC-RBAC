TypeScript-first repo. Do not use any. Keep edits small and composable. Keep app-frontend on official unibest structure. Keep api-common as the shared request/type boundary. Backend rules: keep RBAC real and DB-backed, keep route handlers thin, put business logic in services, and put scheduled work in apps/backend-jobs. Navigation rules: backend menu tree is the source of truth for hierarchy/order/icons/path/permission binding; use definePage only for page-local meta such as title/caption/description/keepAlive/cacheName/code; do not reintroduce page JSON navigation ownership. Frontend rules: reuse PageScaffold, WorkbenchTabs, WorkbenchSettings, MenuTreeNav, UnoIcon, and ContextMenuHost instead of page-local clones; prefer modal-based destructive/configuration flows when inline tree editing gets noisy. UI rules: favor dense professional admin spacing; tune tokens, shell, and workbench layers before per-page spacing hacks; keep sidebar/header/tabs/content visually integrated and responsive. Verification rules: run the relevant package lint/build/test commands before considering work complete.
Frontend page composition rule: for CRUD-style admin pages that include search/filter forms, lists/tables, create-or-update forms, or detail views, create a page-local `components/` directory under that view folder. Extract the search toolbar, list/table, editor dialog or drawer, and detail drawer/panel into page-local components as applicable. Keep the page SFC focused on page state, data loading, permission checks, and action orchestration rather than detailed UI structure.

2026-03-22 addendum: user-facing copy must stay concise and task-oriented; do not show framework理念, prompt leftovers, mock/dev state, auth mode labels, or other implementation detail on UI. For app-frontend specifically, keep a native mobile/mini-program feel, prefer Wot UI as the shared design system, avoid bulky card-like rounded containers, keep auth flows minimal and polished, and ensure list pages use real pagination plus backend-backed persistence for user settings.