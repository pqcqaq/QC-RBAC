2026-03-21: standardized web-frontend CRUD-style views around page-local components. Users, roles, permissions, audit, explorer, and menus now follow the pattern of a thin page SFC plus a local components/ directory. Menu management also split editor/delete dialogs into dedicated components; explorer now uses toolbar/content components. Verification passed with `pnpm --filter @rbac/web-frontend lint` and `pnpm --filter @rbac/web-frontend build`.